import FeaturedStack from "./FeaturedStack";

import "../styles/Hero.css";

const Hero = () => {
  return (
    <section id="hero" className="hero">
      <FeaturedStack />
    </section>
  );
};

export default Hero;
