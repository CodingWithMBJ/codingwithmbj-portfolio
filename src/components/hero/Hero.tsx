import FeaturedStack from "../featuredStack/FeaturedStack";

import "./Hero.css";

const Hero = () => {
  return (
    <section className="hero">
      <FeaturedStack />
    </section>
  );
};

export default Hero;
